# Load dataset into Pandas DataFrame
import pandas as pd
import seaborn as sns
import matplotlib.pyplot as plt

# Load the data
df = pd.read_csv("./final proj/twitchdata-update.csv")

# Store column names for use
features = ['Stream time(minutes)', 'Peak viewers', 'Average viewers', 'Followers', 
            'Followers gained', 'Views gained', 'Partnered', 'Mature']
label = 'Watch time(Minutes)'

# Create feature DataFrame (X) and label Series (y)
X = df[features]
y = df[label]

# Preprocessing
# Convert boolean columns to numeric (1/0)
X['Partnered'] = X['Partnered'].astype(int)
X['Mature'] = X['Mature'].astype(int)

# Drop rows with missing values (if any)
X = X.dropna()
y = y[X.index]  # Ensure label aligns with features

# Dimensions of feature set
print("\nFeature Set Dimensions (Rows, Columns):")
print(X.shape)

# Label dimensions
print("\nLabel Dimensions (Rows):")
print(y.shape)

# Descriptive statistics (rounded to 3 decimal places)
print("\nDescriptive Statistics for Features:")
print(X.describe().round(3))

print("\nDescriptive Statistics for Label:")
print(pd.DataFrame(y.describe()).transpose().round(3))

# Visualize distributions
plt.figure(figsize=(15, 10))
for i, col in enumerate(X.columns):
    plt.subplot(3, 3, i+1)
    sns.histplot(X[col], kde=True)
    plt.title(col)
plt.tight_layout()
plt.show()

plt.figure(figsize=(8, 5))
sns.histplot(y, kde=True)
plt.title('Watch Time Distribution')
plt.show()

# Calculate correlations with label (rounded to 3 decimal places)
print("\nCorrelation with Label (Watch Time):")
label_correlations = X.corrwith(y).round(3)
print(label_correlations.sort_values(ascending=False))

# Calculate feature correlations with each other (rounded to 3 decimal places)
print("\nFeature Correlation Matrix:")
feature_correlations = X.corr().round(3)
plt.figure(figsize=(10, 8))
sns.heatmap(feature_correlations, annot=True, cmap='coolwarm', fmt=".3f")  # Changed to .3f
plt.title("Feature Correlation Matrix")
plt.show()